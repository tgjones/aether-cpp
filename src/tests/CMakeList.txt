# Make sure the compiler can find include files from the engine source code. 
include_directories (${AETHER_SOURCE_DIR}/engine) 

# Add executable called "aether_tests" that is built from the Aether source files,
# combined with the test source files. 
# "demo.cxx" and "demo_b.cxx". The extensions are automatically found. 
add_executable (aether_tests demo.cxx demo_b.cxx) 

# Add test
add_test (aether_tests ${AETHER_BINARY_DIR}/aether_tests)
